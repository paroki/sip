doctrine:
    orm:
        mappings:
            Security:
                is_bundle: false
                dir: '%kernel.project_dir%/src/Security/Entity'
                prefix: 'SIP\Security\Entity'
                alias: Security

api_platform:
    mapping:
        paths:
            - '%kernel.project_dir%/src/Security/Entity'
